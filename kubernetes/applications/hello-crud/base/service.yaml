apiVersion: v1
kind: Service
metadata:
  name: hello-crud
  labels:
    app.kubernetes.io/name: hello-crud
spec:
  selector:
    app.kubernetes.io/name: hello-crud
  ports:
    - name: http
      port: 80
      targetPort: http

